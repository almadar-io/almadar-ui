<!-- Gap ledger for this repo: the source of truth for its open gaps. Managed with scripts/gaps-ledger.mjs in the Almadar monorepo. -->
# @almadar/ui — open gaps

Every open gap this repo owns lives here. This file is the source of truth; the monorepo's `docs/Almadar_Gaps.md` only rolls it up.

- **One entry per gap:** `- **<code>** — <what is wrong and where>. <owning package> [mechanical|architectural] — <evidence, prevention rung>`. `[mechanical]` = small and well-scoped; `[architectural]` = needs design judgment.
- **Codes:** new gaps use this repo's prefix `G-UI-`. Take the "Next code" below, then bump it in the same edit. Codes are never reused or renamed.
- **Close by deleting.** Remove the entry in the same commit as the fix. There is no "closed" section; git history is the record.
- **Cross-repo gaps don't go here.** If fixing it needs another repo, describe it in your report or PR body; the monorepo coordinator files it.

Next code: `G-UI-016`

## Open gaps

### UI tier (`@almadar/ui`)

- **G-UI-001** — RE-VERIFIED 2026-09-21 (live `lolo-ui` re-run, still real, more precise than originally logged): 4 components still hit the generator's `UNRESOLVED` fallback. Their TOP-LEVEL props already look typed (`CodeBlock.naturalLanguages?: readonly LanguageCode[]`, `SubagentTracePanel.subagents: TraceSubagent[]`/`.coordinatorMessages?: readonly TraceChatMessage[]`, `Input`/`InputGroup.onChange?: React.ChangeEventHandler<...>`) — the actual unresolved shapes are ONE LEVEL DEEPER and need real design judgment, not just "add an interface": `code-block.naturalLanguages` (is `LanguageCode` a huge external string-literal union the generator can't flatten — needs checking), `input`/`input-group.CHANGE` (need a `.lolo`-facing payload shape extracted from the raw `React.ChangeEventHandler` generic, e.g. `{value: string}`), `subagent-trace-panel.coordinatorMessages.args` (a `TraceChatMessage`'s own `args` field is `JsonValue`/`ToolArgs`-typed — genuinely dynamic tool-call args, may need a `nonAuthorable` tag rather than a closed shape), `subagent-trace-panel.subagents.timeline` (a `TraceSubagent`'s own `timeline` field has no declared shape). `[needs per-field design judgment before assignment — not purely mechanical]` — orig: `Almadar_Std_UI_Pattern_Type_Gaps.md`
- **G-UI-002** — RE-AUDITED 2026-09-21: of the original 24 "no payload contract" props, most turned out to be CORRECTLY generic by design, not gaps — `avatar`/`box`/`button`/`card`/`choice-button`/`floating-action-button`/`hstack`/`overlay`/`stack`/`vstack`'s `ACTION` (9 sites) all share Button's `action`+`actionPayload` idiom where the payload is deliberately author-supplied at the `.lolo` call site, and `pull-to-refresh`/`REFRESH` + `data-grid`+`infinite-scroll-sentinel`/`LOAD_MORE` (3 sites) follow the same pattern. FIXED one genuine case: `textarea`'s `ACTION` DOES have a hardcoded fixed payload (`{ value: string }`, unlike the generic-payload family) — retyped `action?: EventKey` → `action?: EventEmit<{ value: string }>` in `Textarea.tsx`, full pipeline re-run (ui build → patterns → core build → lolo-ui), confirmed the report count dropped 24→23 with `textarea`/`ACTION` gone. Marginal, likely-not-worth-it (always-empty payload, not "shapeless-hiding-a-real-shape"): `input`/`input-group`'s `CLEAR` and `form`/`form-section`'s `CANCEL` (4 sites, `eventBus.emit` called with no/`{}` argument). NOT YET individually verified: `data-list`'s `LONG_PRESS`/`REORDER`/`SWIPE_LEFT`/`SWIPE_RIGHT` and `entity-list`'s `ITEM_ACTIONS` (5 sites) — these are interaction-specific (not generic passthrough) so more likely than the rest to have a real discoverable shape; check each individually before assuming mechanical. The 7 narrowing-twin sites (`branching-logic-builder`, `game-audio-cue`, `select`, …) are unchanged — still need the not-yet-built mechanism designed first. `[5 sites need individual verification; 7 sites need new mechanism design first; rest confirmed correct-as-is or already fixed]` — orig: `Almadar_Std_UI_Pattern_Type_Gaps.md`
- **G-UI-004** — Sprite-rigger-class editor affordances aren't `.lolo`-authorable yet. `[medium-high, needs design]` — orig: `Almadar_UI_Gaps.md`
- **G-UI-015** — `useCircuitKernel` rebuilds the circuit store whenever the active trait SET changes, discarding the state of traits that stay mounted, so they re-fire their INIT (pinned in `test/mount-lifecycle-once.test.ts`). On real page navigation this is correct (every trait remounts, Runtime Spec Clause 4.1). It is only wrong for an in-page trait-set change, which no host produces today. Fix: carry surviving traits' states into the rebuilt store. Array-identity churn no longer rebuilds the store at all (keyed on the trait set since 2026-09-24). Prevention rung: none — client lifecycle. `[mechanical]` — orig: Almadar_LOLO_Frictions_PLAN Phase 4
