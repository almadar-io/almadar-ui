/**
 * Re-export of the renderer-agnostic trait → orbital mapping now owned by
 * `@almadar/runtime/ui`. Kept here for backward compatibility during the POC.
 *
 * @packageDocumentation
 */
export {
  buildOrbitalsByTrait,
  type ResolvedPageTraits,
} from '@almadar/runtime/ui';
