/**
 * Re-export of the renderer-agnostic @trait.X static analysis now owned by
 * `@almadar/runtime/ui`. Kept here for backward compatibility during the POC.
 *
 * @packageDocumentation
 */
export {
  collectTraitRefsFromResolvedTrait,
  collectEmbeddedTraits,
} from '@almadar/runtime/ui';
