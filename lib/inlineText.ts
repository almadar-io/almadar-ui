/**
 * The attribute a component puts on the element that shows one of its text
 * props (value: the prop name), so an editor can edit that text in place and
 * write it back to exactly that prop.
 */
export const INLINE_TEXT_ATTR = 'data-inline-text';
