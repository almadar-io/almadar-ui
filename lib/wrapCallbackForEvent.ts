/**
 * Re-export of the renderer-agnostic callback wrapper now owned by
 * `@almadar/runtime/ui`. Kept here for backward compatibility during the POC.
 *
 * @packageDocumentation
 */
export { wrapCallbackForEvent } from '@almadar/runtime/ui';
