name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck --if-present
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'

      - name: Build
        run: npm run build --if-present
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'

      - name: Test
        run: npm test --if-present

      - name: Lint
        run: npx eslint --no-warn-ignored --max-warnings 0 components/ hooks/ providers/ context/ stores/ renderer/ lib/ types/

  # validate-service disabled (service .orb schemas need fixing)
  # validate-service:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Validate service schema
  #       run: echo "Disabled"
