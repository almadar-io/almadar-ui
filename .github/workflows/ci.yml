name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm --if-present typecheck
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'

      - name: Build
        run: pnpm --if-present build
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'

      - name: Test
        run: pnpm --if-present test

      - name: Lint
        run: npx eslint --no-warn-ignored --max-warnings 0 components/ hooks/ providers/ context/ renderer/ lib/ types/

  # validate-service disabled (service .orb schemas need fixing)
  # validate-service:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Validate service schema
  #       run: echo "Disabled"
